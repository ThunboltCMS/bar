<h1>Temp</h1>

<div style="max-height: 400px; overflow-y: auto">
	<table>
		<thead>
			<tr>
				<th>Name</th>
				<th>Actions</th>
			</tr>
		</thead>
		<tbody>
			<?php foreach ($cache as $row): ?>
				<tr>
					<td><?php echo $row->getBaseName(); ?></td>
					<td><a href="<?php echo $this->fastLink('cache', $row->getBaseName()) ?>">Truncate</a></td>
				</tr>
			<?php endforeach; ?>
			<tr>
				<th colspan="2">Files</th>
			</tr>
			<?php foreach ($files as $row): ?>
				<tr>
					<td><?php echo $row->getBaseName(); ?></td>
					<td><a href="<?php echo $this->fastLink('cacheFile', $row->getBaseName()) ?>">Delete</a></td>
				</tr>
			<?php endforeach; ?>
			<tr>
				<th colspan="2">Directories</th>
			</tr>
			<?php if ($hasProxies): ?>
			<tr>
				<td>proxies</td>
				<td><a href="<?php echo $this->fastLink('proxiesDir'); ?>">Truncate</a></td>
			</tr>
			<?php endif; ?>
			<tr>
				<th colspan="2">Actions</th>
			</tr>
			<tr>
				<td></td>
				<td><a href="<?php echo $this->fastLink('cacheAll', 'yes'); ?>">Truncate all</a></td>
			</tr>
			<?php if ($hasMemcached): ?>
			<tr>
				<td></td>
				<td><a href="<?php echo $this->fastLink('flushMemcached', 'yes'); ?>">Flush memcached</a></td>
			</tr>
			<?php endif; ?>
		</tbody>
	</table>
</div>
